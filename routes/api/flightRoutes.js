const router = require('express').Router()

// router.post('/planet/:id', login)
// router.post('/rocket/:id', login)
// router.post('/amenity/:id', login)
// router.delete('/planet/:id', login)
// router.post('/timestamp/', login)
// router.delete('/reset', signup)

module.exports = router
